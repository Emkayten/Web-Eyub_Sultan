# Requirements
Install all what that is in requirements.txt

# venv Commands

add venv:

```powershell
python -m venv venv 
```

activate venv:

```powershell
venv\Scripts\Activate
```

deactivate venv:#

```powershell
deactivate
```
 